const{readFile,writeFile}=require("./utils");function renderInPlaceSync(a,b){let c=readFile(a);Object.keys(b).forEach(a=>{c=c.replaceAll(new RegExp(`{{{${a}}}}`,"gi"),b[a])}),writeFile(a,c)}module.exports={renderInPlaceSync};